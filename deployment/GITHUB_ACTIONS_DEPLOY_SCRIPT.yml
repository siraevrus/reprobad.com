# Устарело: деплой настроен в репозитории в `.github/workflows/ci-cd.yml`.
#
# На сервере выполняется: `git pull` в `/var/www/repro`, затем `bash ./server-update.sh --skip-pull`
# (composer, migrate, optimize, php-fpm, sitemap — см. `server-update.sh` в корне репо).
